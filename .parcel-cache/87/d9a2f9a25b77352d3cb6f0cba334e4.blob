var total = document.getElementsByClassName('total')
console.log(total.length)
total.forEach(function set(object) {
    var cards = object.parentElement.querySelectorAll('div.cards .card');

    total.textContent = cards.length;
})